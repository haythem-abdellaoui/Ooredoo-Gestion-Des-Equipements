<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Ooredoo Login</title>
  <link rel="stylesheet" href="{{asset ('css/style_inscription.css')}} ">
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
</head>
<body>
  <div class="login-container">
    <div class="logo">
      <img src="{{ asset ('images/ooredoo_logo-modified.png')}} " alt="Logo Ooredoo" class="ooredoo-logo" />
    </div>
    <form class="login-form" method="POST" novalidate>
      <h2>Créer Un Compte</h2>
      <div class="input-row">
        <div class="input-group">
          <input type="text" id="nom" name="nom" required autocomplete="off" >
          <label for="nom">Nom</label>
          <div class="error-message" id="error-nom">{{ erreurs.nom|default('') }}</div>
        </div>
        <div class="input-group">
          <input type="text" id="prenom" name="prenom" required autocomplete="off" >
          <label for="prenom">Prénom</label>
          <div class="error-message" id="error-prenom">{{ erreurs.prenom|default('') }}</div>
        </div>
      </div>
      <div class="input-row">
      <div class="input-group">
        <input type="text" id="email" name="email" required autocomplete="off" >
        <label for="email">Email</label>
        <div class="error-message" id="error-email">{{ erreurs.email|default('') }}</div>
      </div>

      <div class="input-group">
        <input type="text" id="username" name="username" required autocomplete="off" value="{{ app.request.get('username') }}">
        <label for="username">Nom d'utilisateur</label>
        <div class="error-message" id="error-username">{{ erreurs.username|default('') }}</div>
      </div>
      
      </div>
      <div class="input-group">
        <input type="password" id="password" name="password" required autocomplete="off">
        <label for="password">Mot de passe</label>
        <div class="error-message" id="error-password">{{ erreurs.password|default('') }}</div>
        <button type="button" class="toggle-password" tabindex="-1" aria-label="Afficher ou masquer le mot de passe">
          <svg viewBox="0 0 24 24" width="22" height="22" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M1 12C2.73 7.61 7.11 4.5 12 4.5C16.89 4.5 21.27 7.61 23 12C21.27 16.39 16.89 19.5 12 19.5C7.11 19.5 2.73 16.39 1 12Z" stroke="#ff0033" stroke-width="2"/>
            <circle cx="12" cy="12" r="3.5" stroke="#ff0033" stroke-width="2"/>
          </svg>
        </button>
      </div>
      <button type="submit" class="login-btn">Créez Votre Compte</button>
      <button type="button" class="signup-btn"><a href="{{path ('login')}}" style="text-decoration:none; color:inherit;"> Vous avez déja un compte ? Se Connecter </a></button>
    </form>
    <div class="bubbles">
      <span></span><span></span><span></span><span></span><span></span>
    </div>
  </div>
  <script src="{{asset ('javascript/script.js')}} "></script>
  
</body>
</html> 